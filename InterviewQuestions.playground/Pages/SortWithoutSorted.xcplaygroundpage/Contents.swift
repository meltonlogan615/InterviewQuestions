import Foundation

/**
 Create a `[String]` with no less than 3 elements.
 Create a method that will receive `[String]` and return the same, but in alphabetical order

 example: `["john", "paul", "george", "ringo"]` -> `["george", "john", "paul", "ringo"]`

Do not use `.sort()` or `.sorted()`
*/
